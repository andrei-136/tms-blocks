<?php
// If uninstall not called from WordPress, exit.
if ( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) {
    exit();
}

// Example: Delete plugin options or custom tables here.
// delete_option( 'tmsblocks_some_option' );
// global $wpdb;
// $wpdb->query( "DROP TABLE IF EXISTS {$wpdb->prefix}tmsblocks_custom_table" );

// Add any other cleanup code as needed.
