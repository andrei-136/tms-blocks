# shared

Use this package for code or tooling that should be reused across `tms-blocks` and `tms-blocks-pro`.

Suggested contents:

- shared editor utilities/components published as an internal package
- shared build helpers
- shared schema/constants
- documentation for stable extension points
